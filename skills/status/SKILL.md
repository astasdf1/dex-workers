---
name: status
description: Show eligible external workers, dex-usage cache availability, advisory route, and managed active runs.
allowed-tools: Bash
disable-model-invocation: true
---

Run `${CLAUDE_PLUGIN_ROOT}/bin/dex-workers status` and summarize its structured result without exposing executable configuration beyond the reported paths.
